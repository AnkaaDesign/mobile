# Mobile Application Environment Variables - EXAMPLE
# Copy this file to .env.development, .env.staging, or .env.production
# Expo requires all client-side env vars to be prefixed with EXPO_PUBLIC_

# API Configuration
# Primary API URL (domain) - tried first when online
# Staging:     https://api.staging.ankaadesign.com.br
# Production:  https://api.ankaadesign.com.br
EXPO_PUBLIC_API_URL="https://api.ankaadesign.com.br"

# Fallback API URL (local network) - used when primary is unreachable (offline mode)
# Set this to your local network server IP for automatic fallback
# This allows the app to work when not connected to the internet
# Note: Also configure in app.json extra.fallbackApiUrl for production builds
EXPO_PUBLIC_FALLBACK_API_URL="http://192.168.10.169:3030"

# Application Configuration
EXPO_PUBLIC_APP_NAME="Ankaa Mobile"
EXPO_PUBLIC_APP_ENV="development"

# Expo Project ID (for push notifications)
EXPO_PROJECT_ID=your-expo-project-id

# Deep Linking
EXPO_PUBLIC_APP_SCHEME=ankaadesign
EXPO_PUBLIC_WEB_URL=https://ankaadesign.com.br
