# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment files - prevent ALL env files except examples
.env*
!.env.example
# .env.production holds only public EXPO_PUBLIC_* URLs (baked into the client
# bundle anyway) and MUST travel to the Mac build machine so release archives
# resolve the HTTPS production API instead of leaking the dev LAN URL.
!.env.production

# Coverage
coverage/
.nyc_output/

# Temporary files
*.tmp
*.temp
.cache/
.metro-health-check-*

# React Native / Expo
.expo/
.expo-shared/
web-build/

# macOS
.DS_Store

# Android
android/android/
android/app/build/
android/.gradle/
android/build/
android/local.properties
android/app/debug.keystore
# Release signing keystore — keep the binary keystore AND its passwords out of git.
# The permanent keystore lives at mobile/credentials/ankaa-release.keystore and is
# matched by *.keystore below; its passwords live in credentials/keystore.properties.
# The README + *.example template ARE tracked so the convention is documented.
android/app/ankaa-release.jks
*.jks
*.keystore
credentials.json
credentials/keystore.properties
!credentials/README.md
!credentials/keystore.properties.example
android/gradle/wrapper/gradle-wrapper.jar

# iOS
ios/Pods/
ios/build/
ios/.xcode.env.local

# Build output
build/

# Android root-level config files (generated)
android/app.json
android/package.json

# Documentation (temporary build files)
BUILD_*.md
STYLESHEETFIX_*.md
FIX_*.md
*.sh
# Release/publish tooling — tracked despite the blanket *.sh ignore above.
!scripts/publish-install.sh
!scripts/generate-release-keystore.sh
.claude/
