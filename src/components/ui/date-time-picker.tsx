// Re-export DatePicker as DateTimePicker for backward compatibility
// The DatePicker component handles date, time, and datetime via the `type` prop
export { DatePicker as DateTimePicker } from './date-picker';
