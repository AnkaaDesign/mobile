import { Layout } from '@/components/list/Layout'
import { schedulesListConfig } from '@/config/list/hr/schedules'

export default function SchedulesListScreen() {
  return <Layout config={schedulesListConfig} />
}
