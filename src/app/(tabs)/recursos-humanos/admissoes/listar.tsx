import { Layout } from '@/components/list/Layout'
import { admissionsListConfig } from '@/config/list/hr/admissions'

export default function AdmissionListScreen() {
  return <Layout config={admissionsListConfig} />
}
