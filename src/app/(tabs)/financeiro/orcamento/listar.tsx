import { Layout } from '@/components/list/Layout'
import { budgetListConfig } from '@/config/list/financial/budget'

export default function BudgetListScreen() {
  return <Layout config={budgetListConfig} />
}
