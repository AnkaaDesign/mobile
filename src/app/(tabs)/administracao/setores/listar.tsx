import { Layout } from '@/components/list/Layout'
import { sectorsListConfig } from '@/config/list/administration/sectors'

export default function SectorListScreen() {
  return <Layout config={sectorsListConfig} />
}
