import { Layout } from '@/components/list/Layout'
import { representativesListConfig } from '@/config/list/administration/representatives'

export default function RepresentativeListScreen() {
  return <Layout config={representativesListConfig} />
}
