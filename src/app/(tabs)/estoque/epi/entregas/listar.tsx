import { Layout } from '@/components/list/Layout'
import { ppeDeliveriesInventoryListConfig } from '@/config/list/inventory/ppe-deliveries'

export default function InventoryPpeDeliveriesListScreen() {
  return <Layout config={ppeDeliveriesInventoryListConfig} />
}
