import { Layout } from '@/components/list/Layout'
import { ppeItemsListConfig } from '@/config/list/inventory/ppe-items'

export default function InventoryPpeItemsListScreen() {
  return <Layout config={ppeItemsListConfig} />
}
