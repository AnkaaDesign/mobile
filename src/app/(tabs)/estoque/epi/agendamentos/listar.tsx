import { Layout } from '@/components/list/Layout'
import { ppeSchedulesInventoryListConfig } from '@/config/list/inventory/ppe-schedules'

export default function InventoryPpeSchedulesListScreen() {
  return <Layout config={ppeSchedulesInventoryListConfig} />
}
