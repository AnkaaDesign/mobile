import { Layout } from '@/components/list/Layout'
import { vacationsListConfig } from '@/config/list/personnel-department/vacations'

export default function VacationListScreen() {
  return <Layout config={vacationsListConfig} />
}
