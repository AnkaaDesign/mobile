import { Layout } from '@/components/list/Layout'
import { catalogListConfig } from '@/config/list/painting/catalog'

export default function CatalogListScreen() {
  return <Layout config={catalogListConfig} />
}
