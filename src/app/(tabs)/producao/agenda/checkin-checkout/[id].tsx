// Re-export the shared checkin-checkout screen for agenda route
// This ensures the correct URL pattern: /producao/agenda/checkin-checkout/:id
export { default } from "../../cronograma/checkin-checkout/[id]";
