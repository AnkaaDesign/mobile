import { Layout } from '@/components/list/Layout'
import { cutsListConfig } from '@/config/list/production/cuts'

export default function CuttingListScreen() {
  return <Layout config={cutsListConfig} />
}
