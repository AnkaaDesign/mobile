import { routeToMobilePath } from "@/lib/route-mapper";

/**
 * Maps a route constant to a mobile path
 * This is an alias to maintain backward compatibility
 */
export const routeMapper = routeToMobilePath;
