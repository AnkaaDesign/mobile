# Architecture Diagram - Column Visibility System

**Version:** 2.0 (After Fix)
**Date:** November 2, 2025
**Status:** Target Architecture

---

## 🎯 System Overview

This document visualizes the column visibility architecture before and after the fix.

---

## ❌ BEFORE: Fragmented Architecture (Current State)

```
┌─────────────────────────────────────────────────────────────────────┐
│                         LIST PAGES (50+)                            │
└─────────────────────────────────────────────────────────────────────┘
                                  │
                    ┌─────────────┼─────────────┐
                    ▼             ▼             ▼
        ┌──────────────┐  ┌──────────────┐  ┌──────────────┐
        │  Page 1:     │  │  Page 2:     │  │  Page 3:     │
        │  Task List   │  │  Order List  │  │  Item List   │
        └──────────────┘  └──────────────┘  └──────────────┘
                │                 │                 │
        ┌───────┴────────┐ ┌─────┴──────┐ ┌────────┴──────┐
        │ Import from:   │ │ Import from:│ │ Import from: │
        │ Manager ✅     │ │ Drawer ❌   │ │ Manager ✅    │
        └────────────────┘ └─────────────┘ └───────────────┘
                │                 │                 │
                ▼                 ▼                 ▼
    ┌──────────────────┐  ┌──────────────┐  ┌─────────────┐
    │ column-manager.ts│  │ *-drawer.tsx │  │ manager.ts  │
    │                  │  │              │  │             │
    │ getDefault...()  │  │ getDefault() │  │ getDefault()│
    │      ✅          │  │    ❌ DUP    │  │     ✅      │
    └──────────────────┘  └──────────────┘  └─────────────┘
                │                 │                 │
    ┌───────────┴─────────────────┴─────────────────┴───────────┐
    │                                                             │
    ▼                                                             ▼
┌────────────────┐                                    ┌───────────────────┐
│ Custom Drawer  │◄───────────────────────────────────┤ Generic Drawer UI │
│ Component (25+)│                                    │  (Not Used)       │
│     ❌         │                                    │                   │
└────────────────┘                                    └───────────────────┘

Problems:
❌ Duplicate getDefaultVisibleColumns() in 58 places
❌ Inconsistent import sources (manager vs drawer)
❌ 25+ custom drawer components (should be 1)
❌ Some pages don't know which to use
❌ -v2 files exist alongside v1 (confusion)
```

---

## ✅ AFTER: Unified Architecture (Target State)

```
┌─────────────────────────────────────────────────────────────────────┐
│                         LIST PAGES (50+)                            │
│                    All wrapped in UtilityDrawerWrapper             │
└─────────────────────────────────────────────────────────────────────┘
                                  │
                                  │ All pages import from manager
                                  │ All pages use generic drawer
                                  │
                    ┌─────────────┼─────────────┐
                    ▼             ▼             ▼
        ┌──────────────┐  ┌──────────────┐  ┌──────────────┐
        │  Task List   │  │  Order List  │  │  Item List   │
        │              │  │              │  │              │
        │  import from:│  │  import from:│  │  import from:│
        │  manager ✅  │  │  manager ✅  │  │  manager ✅  │
        └──────────────┘  └──────────────┘  └──────────────┘
                │                 │                 │
                └─────────────────┼─────────────────┘
                                  │
                                  ▼
                    ┌───────────────────────────────┐
                    │   DOMAIN LAYER                │
                    │   (20 Manager Files)          │
                    └───────────────────────────────┘
                                  │
            ┌─────────────────────┼─────────────────────┐
            ▼                     ▼                     ▼
    ┌────────────────┐  ┌──────────────────┐  ┌────────────────┐
    │ task-column-   │  │ order-column-    │  │ item-column-   │
    │ manager.ts     │  │ manager.ts       │  │ manager.ts     │
    │                │  │                  │  │                │
    │ getDefault...()│  │ getDefault...()  │  │ getDefault...()│
    │   ✅ ONLY      │  │   ✅ ONLY        │  │   ✅ ONLY      │
    └────────────────┘  └──────────────────┘  └────────────────┘
                                  │
                                  │ All pages use same drawer
                                  │
                                  ▼
                    ┌───────────────────────────────┐
                    │   UI LAYER                    │
                    │   (1 Generic Component)       │
                    └───────────────────────────────┘
                                  │
                                  ▼
                    ┌───────────────────────────────┐
                    │  ColumnVisibilityDrawerContent│
                    │  from @/components/ui/        │
                    │                               │
                    │  - Generic UI                 │
                    │  - Reusable                   │
                    │  - Configurable               │
                    │       ✅                      │
                    └───────────────────────────────┘
                                  │
                                  ▼
                    ┌───────────────────────────────┐
                    │  useUtilityDrawer() Context   │
                    │  - Manages drawer state       │
                    │  - Single drawer at a time    │
                    └───────────────────────────────┘

Benefits:
✅ Single getDefaultVisibleColumns() per entity (20 total)
✅ All imports from manager files (consistent)
✅ Single generic drawer UI (1 component)
✅ Clear pattern - easy to follow
✅ No confusion or duplication
```

---

## 📁 File Structure Comparison

### BEFORE (Fragmented)

```
src/components/production/task/list/
├── task-table.tsx                          ← Table component
├── column-visibility-manager.ts            ← Has getDefaultVisibleColumns()
├── task-column-visibility-drawer.tsx       ← ALSO has getDefaultVisibleColumns() ❌
└── task-filter-drawer.tsx                  ← Filter component

src/components/production/order/list/
├── order-table.tsx
├── order-column-visibility-drawer.tsx      ← Has getDefaultVisibleColumns() ❌
├── order-column-visibility-drawer-v2.tsx   ← Also has it! Which to use? ❌
└── order-filter-drawer.tsx

src/components/inventory/item/list/
├── item-table.tsx
├── column-visibility-manager.ts            ← Has getDefaultVisibleColumns()
├── column-visibility-drawer.tsx            ← ALSO has it ❌
├── column-visibility-drawer-v2.tsx         ← ALSO has it ❌
└── item-filter-drawer.tsx
```

**Problems:**
- 3-4 files per entity that could have column defaults
- Developer confusion: "Which file should I import from?"
- Duplicate code maintenance
- -v2 files suggest incomplete migration

---

### AFTER (Unified)

```
src/components/production/task/list/
├── task-table.tsx                          ← Table component
├── task-column-manager.ts                  ← ONLY source of getDefaultVisibleColumns() ✅
├── task-filter-drawer.tsx                  ← Filter component
└── index.ts                                ← Barrel export (optional)

src/components/production/order/list/
├── order-table.tsx
├── order-column-manager.ts                 ← ONLY source ✅
├── order-filter-drawer.tsx
└── index.ts

src/components/inventory/item/list/
├── item-table.tsx
├── item-column-manager.ts                  ← ONLY source ✅
├── item-filter-drawer.tsx
└── index.ts

src/components/ui/
├── column-visibility-drawer.tsx            ← Generic drawer used by ALL ✅
└── utility-drawer.tsx                      ← Drawer context
```

**Benefits:**
- Single source of truth per entity
- Clear naming: `{entity}-column-manager.ts`
- Generic drawer used everywhere
- No duplicates, no confusion

---

## 🔄 Data Flow Comparison

### BEFORE: Inconsistent Flow

```
User clicks "Columns" button
        │
        ├─ Page 1: Opens TaskColumnVisibilityDrawer
        │           └─ Gets defaults from task-column-visibility-drawer.tsx ❌
        │
        ├─ Page 2: Opens OrderColumnVisibilityDrawer
        │           └─ Gets defaults from column-visibility-manager.ts ✅
        │
        └─ Page 3: Opens ItemColumnVisibilityDrawer
                    └─ Gets defaults from column-visibility-drawer-v2.tsx ❌

Result: Inconsistent behavior, maintenance nightmare
```

---

### AFTER: Consistent Flow

```
User clicks "Columns" button
        │
        └─ All Pages: openColumnDrawer() from useUtilityDrawer()
                    │
                    └─ Renders ColumnVisibilityDrawerContent
                                │
                                ├─ columns={allColumns}
                                │  (from table's createColumnDefinitions())
                                │
                                └─ defaultColumns={getDefaultVisibleColumns()}
                                   (from {entity}-column-manager.ts)

Result: Consistent behavior, easy maintenance
```

---

## 🏗️ Component Interaction

### Import Chain

```
┌─────────────────────────────────────────────────────────────────┐
│                     List Page Component                         │
│  (e.g., src/app/(tabs)/producao/cronograma/listar.tsx)        │
└─────────────────────────────────────────────────────────────────┘
                               │
                ┌──────────────┼──────────────┐
                │              │              │
                ▼              ▼              ▼
    ┌─────────────────┐  ┌──────────────┐  ┌──────────────────┐
    │ useUtilityDrawer│  │ TaskTable    │  │ getDefault...()  │
    │ from context    │  │ from table   │  │ from manager     │
    └─────────────────┘  └──────────────┘  └──────────────────┘
            │                   │                    │
            │                   │                    │
            ▼                   ▼                    ▼
    ┌─────────────────────────────────────────────────────────┐
    │          openColumnDrawer(() => (                       │
    │            <ColumnVisibilityDrawerContent               │
    │              columns={createColumnDefinitions()}        │
    │              visibleColumns={visibleColumns}            │
    │              defaultColumns={getDefaultVisibleColumns()}│
    │              onVisibilityChange={handleChange}          │
    │            />                                            │
    │          ))                                              │
    └─────────────────────────────────────────────────────────┘
                               │
                               ▼
                ┌────────────────────────────┐
                │ ColumnVisibilityDrawerContent│
                │ from @/components/ui/       │
                │                             │
                │ Props:                      │
                │ - columns                   │
                │ - visibleColumns            │
                │ - defaultColumns            │
                │ - onVisibilityChange        │
                └────────────────────────────┘
```

---

## 📊 State Management

### Column Visibility State Flow

```
┌───────────────────────────────────────────────────────────────┐
│                    Initial State                              │
│   visibleColumnKeys = Array.from(getDefaultVisibleColumns()) │
└───────────────────────────────────────────────────────────────┘
                               │
                               ▼
┌───────────────────────────────────────────────────────────────┐
│                    Render Table                               │
│   <TaskTable visibleColumnKeys={visibleColumnKeys} />        │
│                                                               │
│   Table filters columns to show only visible ones            │
└───────────────────────────────────────────────────────────────┘
                               │
                               ▼
                    User clicks "Columns" button
                               │
                               ▼
┌───────────────────────────────────────────────────────────────┐
│                    Open Drawer                                │
│   openColumnDrawer(() => <ColumnVisibilityDrawerContent />)  │
└───────────────────────────────────────────────────────────────┘
                               │
                               ▼
                    User toggles columns in drawer
                               │
                               ▼
                    User clicks "Apply"
                               │
                               ▼
┌───────────────────────────────────────────────────────────────┐
│                    Update State                               │
│   handleColumnsChange(newColumns)                             │
│   setVisibleColumnKeys(Array.from(newColumns))               │
└───────────────────────────────────────────────────────────────┘
                               │
                               ▼
┌───────────────────────────────────────────────────────────────┐
│                    Re-render Table                            │
│   <TaskTable visibleColumnKeys={newVisibleColumnKeys} />     │
│                                                               │
│   Table updates to show new columns                          │
└───────────────────────────────────────────────────────────────┘
```

---

## 🎨 Visual Component Hierarchy

```
┌────────────────────────────────────────────────────────────────┐
│                    List Page Screen                            │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │ UtilityDrawerWrapper                                     │ │
│  │  ┌────────────────────────────────────────────────────┐ │ │
│  │  │ View (Main Container)                              │ │ │
│  │  │  ┌──────────────────────────────────────────────┐ │ │ │
│  │  │  │ SearchBar + ListActionButton (Columns)       │ │ │ │
│  │  │  └──────────────────────────────────────────────┘ │ │ │
│  │  │  ┌──────────────────────────────────────────────┐ │ │ │
│  │  │  │ TaskTable                                    │ │ │ │
│  │  │  │  - Header Row (columns)                      │ │ │ │
│  │  │  │  - Data Rows (filtered by visibleColumns)   │ │ │ │
│  │  │  └──────────────────────────────────────────────┘ │ │ │
│  │  │  ┌──────────────────────────────────────────────┐ │ │ │
│  │  │  │ FAB (Floating Action Button)                 │ │ │ │
│  │  │  └──────────────────────────────────────────────┘ │ │ │
│  │  └────────────────────────────────────────────────────┘ │ │
│  │                                                          │ │
│  │  When user clicks Columns button:                       │ │
│  │  ┌────────────────────────────────────────────────────┐ │ │
│  │  │ Drawer (slides in from right)                      │ │ │
│  │  │  ┌──────────────────────────────────────────────┐ │ │ │
│  │  │  │ ColumnVisibilityDrawerContent                │ │ │ │
│  │  │  │  - Search bar                                │ │ │ │
│  │  │  │  - Column list with toggles                  │ │ │ │
│  │  │  │  - "Nenhuma" / "Restaurar" buttons           │ │ │ │
│  │  │  │  - "Cancelar" / "Aplicar" buttons            │ │ │ │
│  │  │  └──────────────────────────────────────────────┘ │ │ │
│  │  └────────────────────────────────────────────────────┘ │ │
│  └──────────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────────┘
```

---

## 🔑 Key Design Principles

### 1. Single Responsibility
```
┌─────────────────────────────────────────┐
│ column-visibility-manager.ts            │
│                                         │
│ Responsibility:                         │
│ - Define default visible columns        │
│ - Nothing else                          │
│                                         │
│ ✅ Single source of truth               │
└─────────────────────────────────────────┘
```

### 2. Separation of Concerns
```
┌─────────────────────────────────────────┐
│ Manager (Business Logic)                │
│ - getDefaultVisibleColumns()            │
└─────────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────┐
│ UI Component (Presentation)             │
│ - ColumnVisibilityDrawerContent         │
│ - Renders drawer                        │
│ - Handles user interaction              │
└─────────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────┐
│ Context (State Management)              │
│ - useUtilityDrawer()                    │
│ - Manages drawer open/close state       │
└─────────────────────────────────────────┘
```

### 3. Don't Repeat Yourself (DRY)
```
BEFORE:
┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐
│Drawer│ │Drawer│ │Drawer│ │...×25│  ❌ 25 custom drawers
└──────┘ └──────┘ └──────┘ └──────┘

AFTER:
┌────────────────────────────┐
│ ColumnVisibilityDrawerContent │  ✅ 1 generic drawer
└────────────────────────────┘
         │
    ┌────┼────┬────┬─────┐
    ▼    ▼    ▼    ▼     ▼
  Task Order Item ... All
```

### 4. Convention Over Configuration
```
Standard Pattern (Always the same):

1. Manager file: {entity}-column-manager.ts
2. Import in page: from "@/.../list/{entity}-column-manager"
3. Use generic drawer: <ColumnVisibilityDrawerContent />
4. Wrap page: <UtilityDrawerWrapper>

No decisions needed - follow the pattern!
```

---

## 📐 Scalability

### Adding a New Entity

```
BEFORE (Old Architecture):
1. Create table component (30 min)
2. Create custom drawer component (20 min)
3. Implement getDefaultVisibleColumns in drawer (5 min)
4. Wire up state management (10 min)
5. Style drawer to match others (15 min)
6. Test (10 min)
Total: ~90 minutes

AFTER (New Architecture):
1. Create table component (30 min)
2. Create manager file with getDefaultVisibleColumns (2 min)
3. Copy page template and adjust (3 min)
4. Test (5 min)
Total: ~40 minutes

Improvement: 55% faster! ✅
```

---

## 🧪 Testing Diagram

```
┌────────────────────────────────────────────────────────────┐
│                  Testing Strategy                          │
└────────────────────────────────────────────────────────────┘
                           │
        ┌──────────────────┼──────────────────┐
        ▼                  ▼                  ▼
┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│ Unit Tests   │  │ Integration  │  │   Manual     │
│              │  │    Tests     │  │   Testing    │
└──────────────┘  └──────────────┘  └──────────────┘
        │                  │                  │
        ▼                  ▼                  ▼
┌──────────────────────────────────────────────────┐
│ Test getDefaultVisibleColumns()                  │
│ - Returns Set                                    │
│ - Contains valid column keys                     │
│ - Doesn't include non-existent columns          │
└──────────────────────────────────────────────────┘
        │
        ▼
┌──────────────────────────────────────────────────┐
│ Test drawer opens                                │
│ - Button click opens drawer                      │
│ - Drawer shows all columns                       │
│ - Visible columns are checked                    │
└──────────────────────────────────────────────────┘
        │
        ▼
┌──────────────────────────────────────────────────┐
│ Test column toggle                               │
│ - Toggle changes selection                       │
│ - Apply updates table                            │
│ - Cancel discards changes                        │
│ - Reset returns to defaults                      │
└──────────────────────────────────────────────────┘
```

---

## 🎓 Developer Learning Path

```
New Developer Onboarding:

Step 1: Read QUICK_IMPLEMENTATION_GUIDE.md
   │
   ▼
Step 2: Examine working example (Task)
   │
   ▼
Step 3: Create manager file for new entity
   │
   ▼
Step 4: Copy page template
   │
   ▼
Step 5: Test
   │
   ▼
Done! (~30 min to learn)

vs.

Old Architecture:
Step 1: Find examples (inconsistent)
Step 2: Determine which pattern to use
Step 3: Create custom drawer
Step 4: Debug issues
Step 5: Ask senior dev for help
Step 6: Rewrite
Step 7: Test
Done (~4 hours to learn)

Improvement: 87.5% faster onboarding! ✅
```

---

## 📊 Metrics Dashboard (Conceptual)

```
┌─────────────────────────────────────────────────────────┐
│              Column Visibility Health                   │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  Duplicate Functions:  58 ▓▓▓▓▓▓▓▓▓▓░ → 20 ▓▓░         │
│  Custom Components:    25 ▓▓▓▓▓░░░░░ →  0 ░            │
│  Pattern Consistency:  30% ▓▓▓░░░░░░░ → 100% ▓▓▓▓▓▓▓▓▓▓│
│  Code Lines:        5,000 ▓▓▓▓▓▓▓▓▓▓░ → 2,500 ▓▓▓▓▓░   │
│                                                         │
│  Overall Health:       ⚠️ POOR → ✅ EXCELLENT          │
└─────────────────────────────────────────────────────────┘
```

---

## 🎯 Summary

### The Transformation

**From:** Fragmented, duplicated, confusing architecture
**To:** Unified, maintainable, clear architecture

**Key Changes:**
1. ✅ Single source: `{entity}-column-manager.ts`
2. ✅ Generic UI: `ColumnVisibilityDrawerContent`
3. ✅ Consistent imports: Always from manager
4. ✅ Standard pattern: Easy to follow
5. ✅ No duplication: DRY principle

**Results:**
- 66% less duplication
- 55% faster development
- 87% faster onboarding
- 100% consistency

---

**Document Version:** 1.0
**Created:** November 2, 2025
**Author:** Development Team
